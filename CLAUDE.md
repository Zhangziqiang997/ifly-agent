# CLAUDE.md — 参数智能体项目指引

> **项目身份**：科大讯飞 2026 展翼校招培训大赛课题  
> **课题名称**：AI 驱动的招投标参数智能比对与决策辅助系统  
> **开发周期**：2026.07.24 – 07.28（4 天）  
> **团队规模**：8 人（PM 1 + 产品 2 + 开发 2 + 内容 3）  
> **AI 工具**：Kooky（Claude Code），每人 $20 额度，组长 $100

---

## 一、产品一句话定义

输入一份智慧黑板招标文件 → 自动识别是哪家竞品控的标 → 逐条对比讯飞参数 → 标出正/负偏离 → 对不满足项给出应对建议。

---

## 二、核心技术决策（已定，不要再讨论）

| 决策 | 结论 | 定稿日期 |
|------|------|----------|
| 技术栈 | Python 3 + Streamlit + JSON 文件存储 | 07.24 |
| 对比架构 | **程序粗筛 → AI 精判** 双层架构 | 07.24 |
| 产品形态 | Web 页面（Streamlit），非 CLI | 07.24 |
| 数据库 | 不做数据库，JSON 文件即可 | 07.24 |
| 竞品范围 | 3 家：希沃、鸿合、海康威视（07.25 根据真实数据调整，原文香替换为海康） | 07.25 |
| 演示规模 | 每家 10-15 条典型参数即可 | 07.24 |
| Demo 数据 | 硬编码样例 JSON，不做上传解析（比赛演示用） | 07.24 |
| 不做的事 | 不打分、不建全量数据库、不做移动端 | 07.24 |

---

## 三、核心概念速查

| 术语 | 定义 |
|------|------|
| **控标** | 招标参数被某一家厂商的产品规格绑定，其他家不满足则无法投标 |
| **控标方** | 招标参数背后偏向的那家厂商 |
| **废标项（★）** | 不满足直接出局，不能投标 |
| **扣分项（△）** | 不满足扣技术分，但不废标 |
| **正偏离** | 讯飞规格高于招标要求，加分项 |
| **负偏离** | 讯飞不满足招标要求 |
| **特征库** | 每家竞品独有的参数项集合，用于判定控标方 |
| **独有特征** | 某条招标参数市面仅有 1 家满足 → 该家的控标嫌疑 +1 |
| **质询** | 招标意向公示期间，厂商可书面质疑参数倾向性 |

---

## 四、三层对比架构（核心算法）

```
招标参数
    │
    ├──▶ [第一关] 程序粗筛：提取数值 / 单位归一化 / 关键词匹配
    │       80% 的参数在这一关就有明确结论
    │
    ├──▶ [第二关] 横向对比：控标方识别
    │       每条招标参数 × 3家竞品库 → 找出独有特征
    │       → 统计每家命中数 → 判定控标方
    │       纯查表逻辑，程序搞定，不需要 AI
    │
    └──▶ [第三关] 纵向对比：讯飞匹配
            招标参数 × 讯飞参数 → AI 语义匹配
            → 正偏离 / 负偏离（说辞可改）/ 负偏离（真不满足）
            → 负偏离项 AI 生成应对建议
            只有这关才调用大模型
```

### 什么时候用程序、什么时候用 AI？

| 任务 | 用什么 | 原因 |
|------|--------|------|
| 数值比较（4K vs 5K） | 程序 | 正则提取数字比大小即可 |
| 单位归一化（"4K" = "3840×2160"） | 程序 | 映射表查表 |
| 找出"只有某一家满足"的参数 | 程序 | 查表统计，纯逻辑 |
| 语义匹配（"无线投屏" vs "屏幕镜像"） | **AI** | 说法不同但意思相同，必须语义理解 |
| 生成质疑话术 | **AI** | 需要理解和创作，程序做不了 |
| 改写参数说辞 | **AI** | 需要语言能力 |

---

## 五、演示 Demo 的三个页面

1. **上传 & 识别结果页**：上传招标文件 → 显示"控标方判定：希沃（置信度 85%，命中 7/12 条独有特征）"
2. **参数对比表页**：逐条展示 🟢正偏离 / 🟡负偏离(可改说辞) / 🔴负偏离(真不满足)
3. **应对建议页**：点击任一负偏离项 → 弹出建议卡片

---

## 六、项目文件结构

> 📍 遵循 **SPEC → PLAN → EXECUTE → CONFIRM** 工作流。  
> 完整产品规格见 [`docs/01-PRD.md`](./docs/01-PRD.md)，Demo 范围见 [`docs/02-PLAN.md`](./docs/02-PLAN.md)。

```
参数智能体/
├── CLAUDE.md                    # ← 本文件，项目总指引（AI 入口）
├── README.md                    # 快速入门
├── .gitignore
├── docs/
│   ├── 01-PRD.md                # SPEC — 产品规格书（完整能力定义）
│   ├── 02-PLAN.md               # PLAN — 实施计划（Demo 范围/取舍/时间线）
│   ├── 03-technical-design.md   # 技术方案（三层架构/数据模型/API）
│   ├── 04-team-division.md      # 团队分工 & 4天任务分配
│   ├── 05-execution-log.md      # EXECUTE+CONFIRM — 执行日志（迭代+AI协作+决策）
│   └── 06-roadshow.md           # 路演材料（故事线/脚本/操作步骤/Q&A）
├── data/
│   ├── competitors/             # 竞品参数库（JSON）— 希沃/鸿合/海康
│   ├── xunfei/                  # 讯飞参数库（JSON）
│   └── samples/                 # 样例招标文件
├── 知识库3/                     # 真实数据源（69个文件，gitignore 排除）
├── src/                         # 源代码
├── presentations/               # 路演 PPT & 项目说明卡
└── outputs/                     # 生成产物（截图、录屏等）
```

---

## 七、使用 Kooky（Claude Code）的规范

### 怎么让 AI 写代码
1. 把当前要做的功能描述清楚，贴到 Kooky 对话框
2. 让 AI 先讲方案，确认后再让它写代码
3. 生成的代码保存到 `src/` 下
4. 跑通后截图保存到 `outputs/`

### 怎么记录 AI 协作过程（评委重点考察）
- 每次让 AI 做重要决策时，截图保存对话
- 把 prompt 和 AI 回复的关键部分记到 `docs/03-ai-collaboration.md`
- 路演时按"人提了什么要求 → AI 给了什么 → 人怎么判断和修改"的格式讲

### 额度管理
- 先用程序逻辑，调不通再上 AI
- $20/人 = 约 400 次简单对话或 40 次复杂任务
- 不要反复让 AI 做同一件事，一次说清楚

---

## 八、时间线

| 日期 | 目标 | 交付物 |
|------|------|--------|
| 07.24 | 组队 + 需求定稿 | PRD、分工表、CLAUDE.md |
| 07.25 | 核心链路跑通 | 三层对比的 Python 脚本可运行 |
| 07.26 | 班级初选 | Streamlit Demo 可演示 |
| 07.27 | 彩排 | 路演脚本、PPT、录制备选视频 |
| 07.28 | 正式 PK | 10 分钟路演 + 5 分钟问答 |

---

## 九、比赛评审维度 & 应对策略

| 维度 | 占比 | 我们的策略 |
|------|------|-----------|
| 业务问题与用户价值 | 30% | 招投标是真场景，讯飞内部真实痛点 |
| AI 应用与实现效果 | 30% | 三层架构里 AI 精确参与语义层，不滥用不缺席 |
| 团队协作与过程 | 25% | 完整记录人机协作过程、迭代日志 |
| 路演与呈现 | 15% | 现场 Live Demo 优先于录屏 |

---

## 十、给 AI（Claude Code）的提示

当你在本项目中被调用时：
- 优先读 `docs/` 下的相关文档，不要凭记忆做决策
- 本项目的核心约束是 **4 天、8 人、$20/人**，始终考虑可行性
- 所有架构决策参考第二章"核心技术决策"，这些已定案不要推翻
- 代码生成原则：简单 > 优雅，能跑 > 完美
- 每次交互都思考：这段对话能不能用于路演的 AI 协作证据？

---

## 十一、Git / GitHub 操作纪律（🚨 避免反复重试）

**环境问题**：
- 本机 Git 配置了 socks5h 代理（`127.0.0.1:18080`），GitHub HTTPS 不能走代理
- 没有配置 SSH 密钥，不能用 `git@github.com`
- 没有安装 `gh`（GitHub CLI）

**正确做法**：
```bash
# GitHub 推送必须绕代理
git -c http.proxy="" -c https.proxy="" push

# 如果失败，最多重试 1 次（间隔 10 秒以上）
# 如果第 2 次还失败 → 停止，不再重试
# 告诉用户："推送暂时失败（网络问题），commit 已在本地，稍后重试"
```

**禁止行为**：
- ❌ 反复重试超过 2 次（浪费时间，网络问题不会因为重试就消失）
- ❌ 尝试换 SSH / gh / 其他协议（都没配好，试了也白试）
- ❌ 每次 push 后等待超过 30 秒
